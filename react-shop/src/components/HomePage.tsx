import MainPage from "./MainPage";
import Nav from "./Nav";

const HomePage = () => {
  return(
    <section className="drawer-content">
      <Nav></Nav>
      {/* <MainPage></MainPage> */}
    </section>
  )
};

export default HomePage;